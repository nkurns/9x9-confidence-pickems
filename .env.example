# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/playoffpool?retryWrites=true&w=majority

# JWT secret for authentication (use a long random string in production)
JWT_SECRET=your-secret-key-here

# Port (usually set automatically by hosting platform)
PORT=3000
